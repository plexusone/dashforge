// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/plexusone/dashforge/ent/license"
	"github.com/plexusone/dashforge/ent/predicate"
	"github.com/plexusone/dashforge/ent/principal"
	"github.com/plexusone/dashforge/ent/seatassignment"
)

// SeatAssignmentQuery is the builder for querying SeatAssignment entities.
type SeatAssignmentQuery struct {
	config
	ctx           *QueryContext
	order         []seatassignment.OrderOption
	inters        []Interceptor
	predicates    []predicate.SeatAssignment
	withLicense   *LicenseQuery
	withPrincipal *PrincipalQuery
	withAssigner  *PrincipalQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SeatAssignmentQuery builder.
func (_q *SeatAssignmentQuery) Where(ps ...predicate.SeatAssignment) *SeatAssignmentQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *SeatAssignmentQuery) Limit(limit int) *SeatAssignmentQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *SeatAssignmentQuery) Offset(offset int) *SeatAssignmentQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *SeatAssignmentQuery) Unique(unique bool) *SeatAssignmentQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *SeatAssignmentQuery) Order(o ...seatassignment.OrderOption) *SeatAssignmentQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryLicense chains the current query on the "license" edge.
func (_q *SeatAssignmentQuery) QueryLicense() *LicenseQuery {
	query := (&LicenseClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seatassignment.Table, seatassignment.FieldID, selector),
			sqlgraph.To(license.Table, license.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seatassignment.LicenseTable, seatassignment.LicenseColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPrincipal chains the current query on the "principal" edge.
func (_q *SeatAssignmentQuery) QueryPrincipal() *PrincipalQuery {
	query := (&PrincipalClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seatassignment.Table, seatassignment.FieldID, selector),
			sqlgraph.To(principal.Table, principal.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seatassignment.PrincipalTable, seatassignment.PrincipalColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAssigner chains the current query on the "assigner" edge.
func (_q *SeatAssignmentQuery) QueryAssigner() *PrincipalQuery {
	query := (&PrincipalClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(seatassignment.Table, seatassignment.FieldID, selector),
			sqlgraph.To(principal.Table, principal.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, seatassignment.AssignerTable, seatassignment.AssignerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SeatAssignment entity from the query.
// Returns a *NotFoundError when no SeatAssignment was found.
func (_q *SeatAssignmentQuery) First(ctx context.Context) (*SeatAssignment, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{seatassignment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *SeatAssignmentQuery) FirstX(ctx context.Context) *SeatAssignment {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SeatAssignment ID from the query.
// Returns a *NotFoundError when no SeatAssignment ID was found.
func (_q *SeatAssignmentQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{seatassignment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *SeatAssignmentQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SeatAssignment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SeatAssignment entity is found.
// Returns a *NotFoundError when no SeatAssignment entities are found.
func (_q *SeatAssignmentQuery) Only(ctx context.Context) (*SeatAssignment, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{seatassignment.Label}
	default:
		return nil, &NotSingularError{seatassignment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *SeatAssignmentQuery) OnlyX(ctx context.Context) *SeatAssignment {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SeatAssignment ID in the query.
// Returns a *NotSingularError when more than one SeatAssignment ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *SeatAssignmentQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{seatassignment.Label}
	default:
		err = &NotSingularError{seatassignment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *SeatAssignmentQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SeatAssignments.
func (_q *SeatAssignmentQuery) All(ctx context.Context) ([]*SeatAssignment, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SeatAssignment, *SeatAssignmentQuery]()
	return withInterceptors[[]*SeatAssignment](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *SeatAssignmentQuery) AllX(ctx context.Context) []*SeatAssignment {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SeatAssignment IDs.
func (_q *SeatAssignmentQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(seatassignment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *SeatAssignmentQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *SeatAssignmentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*SeatAssignmentQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *SeatAssignmentQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *SeatAssignmentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *SeatAssignmentQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SeatAssignmentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *SeatAssignmentQuery) Clone() *SeatAssignmentQuery {
	if _q == nil {
		return nil
	}
	return &SeatAssignmentQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]seatassignment.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.SeatAssignment{}, _q.predicates...),
		withLicense:   _q.withLicense.Clone(),
		withPrincipal: _q.withPrincipal.Clone(),
		withAssigner:  _q.withAssigner.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithLicense tells the query-builder to eager-load the nodes that are connected to
// the "license" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeatAssignmentQuery) WithLicense(opts ...func(*LicenseQuery)) *SeatAssignmentQuery {
	query := (&LicenseClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLicense = query
	return _q
}

// WithPrincipal tells the query-builder to eager-load the nodes that are connected to
// the "principal" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeatAssignmentQuery) WithPrincipal(opts ...func(*PrincipalQuery)) *SeatAssignmentQuery {
	query := (&PrincipalClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPrincipal = query
	return _q
}

// WithAssigner tells the query-builder to eager-load the nodes that are connected to
// the "assigner" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *SeatAssignmentQuery) WithAssigner(opts ...func(*PrincipalQuery)) *SeatAssignmentQuery {
	query := (&PrincipalClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAssigner = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		LicenseID uuid.UUID `json:"license_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SeatAssignment.Query().
//		GroupBy(seatassignment.FieldLicenseID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *SeatAssignmentQuery) GroupBy(field string, fields ...string) *SeatAssignmentGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SeatAssignmentGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = seatassignment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		LicenseID uuid.UUID `json:"license_id,omitempty"`
//	}
//
//	client.SeatAssignment.Query().
//		Select(seatassignment.FieldLicenseID).
//		Scan(ctx, &v)
func (_q *SeatAssignmentQuery) Select(fields ...string) *SeatAssignmentSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &SeatAssignmentSelect{SeatAssignmentQuery: _q}
	sbuild.label = seatassignment.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SeatAssignmentSelect configured with the given aggregations.
func (_q *SeatAssignmentQuery) Aggregate(fns ...AggregateFunc) *SeatAssignmentSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *SeatAssignmentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !seatassignment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *SeatAssignmentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SeatAssignment, error) {
	var (
		nodes       = []*SeatAssignment{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withLicense != nil,
			_q.withPrincipal != nil,
			_q.withAssigner != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SeatAssignment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SeatAssignment{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withLicense; query != nil {
		if err := _q.loadLicense(ctx, query, nodes, nil,
			func(n *SeatAssignment, e *License) { n.Edges.License = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPrincipal; query != nil {
		if err := _q.loadPrincipal(ctx, query, nodes, nil,
			func(n *SeatAssignment, e *Principal) { n.Edges.Principal = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAssigner; query != nil {
		if err := _q.loadAssigner(ctx, query, nodes, nil,
			func(n *SeatAssignment, e *Principal) { n.Edges.Assigner = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *SeatAssignmentQuery) loadLicense(ctx context.Context, query *LicenseQuery, nodes []*SeatAssignment, init func(*SeatAssignment), assign func(*SeatAssignment, *License)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeatAssignment)
	for i := range nodes {
		fk := nodes[i].LicenseID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(license.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "license_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeatAssignmentQuery) loadPrincipal(ctx context.Context, query *PrincipalQuery, nodes []*SeatAssignment, init func(*SeatAssignment), assign func(*SeatAssignment, *Principal)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeatAssignment)
	for i := range nodes {
		fk := nodes[i].PrincipalID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(principal.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "principal_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *SeatAssignmentQuery) loadAssigner(ctx context.Context, query *PrincipalQuery, nodes []*SeatAssignment, init func(*SeatAssignment), assign func(*SeatAssignment, *Principal)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SeatAssignment)
	for i := range nodes {
		fk := nodes[i].AssignedBy
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(principal.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "assigned_by" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *SeatAssignmentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *SeatAssignmentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(seatassignment.Table, seatassignment.Columns, sqlgraph.NewFieldSpec(seatassignment.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, seatassignment.FieldID)
		for i := range fields {
			if fields[i] != seatassignment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withLicense != nil {
			_spec.Node.AddColumnOnce(seatassignment.FieldLicenseID)
		}
		if _q.withPrincipal != nil {
			_spec.Node.AddColumnOnce(seatassignment.FieldPrincipalID)
		}
		if _q.withAssigner != nil {
			_spec.Node.AddColumnOnce(seatassignment.FieldAssignedBy)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *SeatAssignmentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(seatassignment.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = seatassignment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SeatAssignmentGroupBy is the group-by builder for SeatAssignment entities.
type SeatAssignmentGroupBy struct {
	selector
	build *SeatAssignmentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *SeatAssignmentGroupBy) Aggregate(fns ...AggregateFunc) *SeatAssignmentGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *SeatAssignmentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeatAssignmentQuery, *SeatAssignmentGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *SeatAssignmentGroupBy) sqlScan(ctx context.Context, root *SeatAssignmentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SeatAssignmentSelect is the builder for selecting fields of SeatAssignment entities.
type SeatAssignmentSelect struct {
	*SeatAssignmentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *SeatAssignmentSelect) Aggregate(fns ...AggregateFunc) *SeatAssignmentSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *SeatAssignmentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SeatAssignmentQuery, *SeatAssignmentSelect](ctx, _s.SeatAssignmentQuery, _s, _s.inters, v)
}

func (_s *SeatAssignmentSelect) sqlScan(ctx context.Context, root *SeatAssignmentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
