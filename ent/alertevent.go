// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/plexusone/dashforge/ent/alert"
	"github.com/plexusone/dashforge/ent/alertevent"
)

// AlertEvent is the model entity for the AlertEvent schema.
type AlertEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Type of event
	EventType alertevent.EventType `json:"event_type,omitempty"`
	// Data that triggered the alert (metric value, etc.)
	TriggerData map[string]interface{} `json:"trigger_data,omitempty"`
	// List of integration slugs that were notified
	ChannelsNotified []string `json:"channels_notified,omitempty"`
	// Number of channels that succeeded
	ChannelsSuccess int `json:"channels_success,omitempty"`
	// Number of channels that failed
	ChannelsFailed int `json:"channels_failed,omitempty"`
	// Error message if event_type=error
	ErrorMessage string `json:"error_message,omitempty"`
	// User who acknowledged (if event_type=acknowledged)
	AcknowledgedBy string `json:"acknowledged_by,omitempty"`
	// When this event occurred
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AlertEventQuery when eager-loading is set.
	Edges        AlertEventEdges `json:"edges"`
	alert_events *int
	selectValues sql.SelectValues
}

// AlertEventEdges holds the relations/edges for other nodes in the graph.
type AlertEventEdges struct {
	// Alert holds the value of the alert edge.
	Alert *Alert `json:"alert,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AlertOrErr returns the Alert value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AlertEventEdges) AlertOrErr() (*Alert, error) {
	if e.Alert != nil {
		return e.Alert, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: alert.Label}
	}
	return nil, &NotLoadedError{edge: "alert"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AlertEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case alertevent.FieldTriggerData, alertevent.FieldChannelsNotified:
			values[i] = new([]byte)
		case alertevent.FieldID, alertevent.FieldChannelsSuccess, alertevent.FieldChannelsFailed:
			values[i] = new(sql.NullInt64)
		case alertevent.FieldEventType, alertevent.FieldErrorMessage, alertevent.FieldAcknowledgedBy:
			values[i] = new(sql.NullString)
		case alertevent.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case alertevent.ForeignKeys[0]: // alert_events
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AlertEvent fields.
func (_m *AlertEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case alertevent.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case alertevent.FieldEventType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_type", values[i])
			} else if value.Valid {
				_m.EventType = alertevent.EventType(value.String)
			}
		case alertevent.FieldTriggerData:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field trigger_data", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.TriggerData); err != nil {
					return fmt.Errorf("unmarshal field trigger_data: %w", err)
				}
			}
		case alertevent.FieldChannelsNotified:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field channels_notified", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.ChannelsNotified); err != nil {
					return fmt.Errorf("unmarshal field channels_notified: %w", err)
				}
			}
		case alertevent.FieldChannelsSuccess:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field channels_success", values[i])
			} else if value.Valid {
				_m.ChannelsSuccess = int(value.Int64)
			}
		case alertevent.FieldChannelsFailed:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field channels_failed", values[i])
			} else if value.Valid {
				_m.ChannelsFailed = int(value.Int64)
			}
		case alertevent.FieldErrorMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error_message", values[i])
			} else if value.Valid {
				_m.ErrorMessage = value.String
			}
		case alertevent.FieldAcknowledgedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field acknowledged_by", values[i])
			} else if value.Valid {
				_m.AcknowledgedBy = value.String
			}
		case alertevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case alertevent.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field alert_events", value)
			} else if value.Valid {
				_m.alert_events = new(int)
				*_m.alert_events = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AlertEvent.
// This includes values selected through modifiers, order, etc.
func (_m *AlertEvent) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAlert queries the "alert" edge of the AlertEvent entity.
func (_m *AlertEvent) QueryAlert() *AlertQuery {
	return NewAlertEventClient(_m.config).QueryAlert(_m)
}

// Update returns a builder for updating this AlertEvent.
// Note that you need to call AlertEvent.Unwrap() before calling this method if this AlertEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AlertEvent) Update() *AlertEventUpdateOne {
	return NewAlertEventClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AlertEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AlertEvent) Unwrap() *AlertEvent {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AlertEvent is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AlertEvent) String() string {
	var builder strings.Builder
	builder.WriteString("AlertEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("event_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.EventType))
	builder.WriteString(", ")
	builder.WriteString("trigger_data=")
	builder.WriteString(fmt.Sprintf("%v", _m.TriggerData))
	builder.WriteString(", ")
	builder.WriteString("channels_notified=")
	builder.WriteString(fmt.Sprintf("%v", _m.ChannelsNotified))
	builder.WriteString(", ")
	builder.WriteString("channels_success=")
	builder.WriteString(fmt.Sprintf("%v", _m.ChannelsSuccess))
	builder.WriteString(", ")
	builder.WriteString("channels_failed=")
	builder.WriteString(fmt.Sprintf("%v", _m.ChannelsFailed))
	builder.WriteString(", ")
	builder.WriteString("error_message=")
	builder.WriteString(_m.ErrorMessage)
	builder.WriteString(", ")
	builder.WriteString("acknowledged_by=")
	builder.WriteString(_m.AcknowledgedBy)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AlertEvents is a parsable slice of AlertEvent.
type AlertEvents []*AlertEvent
